#include "circle.h"
namespace shapes{
    Circle::Circle() : radius(0) {}
    Circle::Circle(float side) : radius(r) {}
    Circle::~Circle() {}

    float Circle::getRadius() const {
        return radius;
    }
    void Circlee::setRadius(float radius) {
        sideRadius = radius;
    }
}